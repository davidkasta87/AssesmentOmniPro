import { test as base } from '@playwright/test'; \nimport { HomePage, ElementsPage, TextBoxPage, FormsPage, PracticeFormPage } from '../page-objects'; \n\n// Extend basic test with page object fixtures\ntype PageFixtures = {\n  homePage: HomePage;\n  elementsPage: ElementsPage;\n  textBoxPage: TextBoxPage;\n  formsPage: FormsPage;\n  practiceFormPage: PracticeFormPage;\n};\n\nexport const test = base.extend<PageFixtures>({\n  homePage: async ({ page }, use) => {\n    const homePage = new HomePage(page);\n    await use(homePage);\n  },\n\n  elementsPage: async ({ page }, use) => {\n    const elementsPage = new ElementsPage(page);\n    await use(elementsPage);\n  },\n\n  textBoxPage: async ({ page }, use) => {\n    const textBoxPage = new TextBoxPage(page);\n    await use(textBoxPage);\n  },\n\n  formsPage: async ({ page }, use) => {\n    const formsPage = new FormsPage(page);\n    await use(formsPage);\n  },\n\n  practiceFormPage: async ({ page }, use) => {\n    const practiceFormPage = new PracticeFormPage(page);\n    await use(practiceFormPage);\n  },\n});\n\nexport { expect } from '@playwright/test';